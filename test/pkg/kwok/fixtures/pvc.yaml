apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kwok-pvc-0
  labels:
    type: kwok
spec:
  storageClassName: kwok-noop
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
