name: Helm Build Template

on:
  workflow_call:
    inputs:
      ref:
        description: 'Git ref to checkout'
        required: false
        type: string
        default: ${{ github.ref }}
      registry:
        description: 'Container registry URL (only used to construct chart metadata)'
        required: true
        type: string
      repo_base:
        description: 'Repository base path'
        required: true
        type: string
      tags:
        description: 'Helm chart tags (newline-separated list)'
        required: true
        type: string
      artifact_retention_days:
        description: 'Retention for uploaded chart artifacts'
        required: false
        type: number
        default: 1

permissions:
  contents: read

jobs:
  build-helm:
    name: build-helm
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
          disable-sudo-and-containers: true

      - name: Check out the code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Build Helm chart packages
        env:
          REGISTRY: ${{ inputs.registry }}
          REPO_BASE: ${{ inputs.repo_base }}
          TAGS: ${{ inputs.tags }}
        run: |
          set -eou pipefail
          while IFS= read -r tag; do
            if [[ -z "${tag}" ]]; then
              continue
            fi
            echo "Building Helm chart for tag: ${tag}"
            make helm-build \
              REGISTRY="${REGISTRY}" \
              REPO_BASE="${REPO_BASE}" \
              TAG="${tag}"
          done <<< "${TAGS}"

      - name: Upload Helm chart artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: helm-charts
          path: dist/*.tgz
          retention-days: ${{ inputs.artifact_retention_days }}
          if-no-files-found: error
